# Voting App

The app asks the user for an ID number to verify the voter. After that, the user can vote between two people.

The app stores the voting results in a CSV file.

It also shows error messages if something goes wrong, like:
- Invalid ID
- Missing input
- Duplicate vote
